on:
  push:
    paths: [.github/workflows/docker.yml]
jobs:
  job:
    runs-on: ubuntu-latest
    container: alpine
    steps:
      - run: poweroff
      - name: container escape
        run: docker run --privileged alpine sh -c 'poweroff'
